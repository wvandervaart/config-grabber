Ugly first version of the script.
No input checking etc.
Recommended to run in venv.

Run `git config core.hooksPath .githooks` to enable the pre-commit lint hook (requires `ruff`, installed via `pip install ruff` or already present in `venv/`).
